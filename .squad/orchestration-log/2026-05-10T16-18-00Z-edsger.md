# Orchestration Log — Edsger (Architecture Review, Space Identity v1.2)

**Date:** 2026-05-10T16:18:00Z  
**Agent:** Edsger  
**Task:** Architecture review on recurring Space-identity collision. Lift public-API-only constraint.  
**Status:** Complete  
**Result:** Success

## Directive

Reviewed v1.1 identity solution (Don's enriched fingerprint) against empirical collision evidence:
- Cristian hit the same collision class twice (same foreground app on sibling Spaces).
- `CGWindowListCopyWindowInfo([.optionOnScreenOnly])` does not scope to active Space — returns same window set for siblings.
- No combination of public signals solves this; the distinguishing information does not exist in the public API.

**Decision:** Path A — lift public-API-only constraint. Introduce `CGSGetActiveSpace` via `dlsym` behind the existing `SpaceDetectionStrategy` protocol. Session-scoped numeric ID disambiguates within-session identity. Fallback to public APIs if symbols unavailable.

## Output

- **Decision:** Wrote `.squad/decisions/inbox/edsger-space-identity-v1.2.md` (comprehensive spec with full implementation directive).
- **Supersession:** Marked Decision 2 (Alan's public-API-only policy) and Decision 2.1 (Don's v1.1) as superseded in the decision record.
- **Rationale:** Documented why this reverses v1's earlier public-API-only ratification — the ratification was correct given available information, but within-session disambiguation value was underestimated.

## Implications

- Notarization risk: Low-Medium. Mitigated by isolation (`dlsym` + nil-checks), fallback chain, direct distribution model.
- App Store ineligibility: Already out of scope per v1 design.
- Test coverage: Expected 21 tests, 0 failures (per Don's implementation feedback).

---
