# Orchestration Log: Don-7

**Timestamp:** 2026-05-10T16:06:03Z  
**Agent:** Don  
**Status:** Success

## Summary

Bug fix: Space identity collision (two Spaces sharing one name). Strengthened SpaceIdentity with frontmostApp bundleID + window count + geometry hash on top of existing displayUUID + windowSignature. Tightened matcher: exact match wins; fuzzy match must clear similarity threshold AND beat runner-up by margin, else returns nil (→ "UNNAMED" → user re-renames). Old store entries effectively orphaned (Codable tolerant load, fingerprint mismatch on re-visit).

**Test Result:** 18 tests, 0 failures

## Outputs

- Decision: Space Identity v1.1 (merged to decisions.md, marked as superseding earlier identity-v1)
- All tests passing; Space identity collision fixed
- Backward-compatible migration: pre-v1.1 entries decode with defaults, become orphaned on re-visit

## Next Steps

- Monitor Space disambiguation in production
- Ready for M3 and beyond
- Old orphaned entries harmless; users re-create labels via Option-click rename as needed
