# Don-9 Orchestration Log

**Date:** 2026-05-10T16:28:42.833-04:00  
**Agent:** Don  
**Status:** Completed  
**Topic:** rename-identity drift fix + retire stale-rebind

## Summary

Fixed "renamed 2 to second, will always show third" regression by:

1. **Removed re-bind-on-launch logic** — Heuristic-only stored entries (`cgsSpaceID = nil`) no longer auto-attach to fresh CGS IDs. This was the stale heuristic-rebind logic from v1.2 that misrouted names to wrong Spaces.

2. **Fresh rename identity capture** — Rename submit now captures `SpaceIdentity` at Enter/commit time using the same `SpaceFingerprinter.currentIdentity()` path used by display refresh, instead of reusing an identity cached at click-down. This fixes the read/write drift bug that allowed stale identity to persist through rename.

## Changes

- **Don-retire-space-rebind.md** decision: Retired heuristic CGS re-bind; established "fresh identity at rename submit" invariant.
- **decisions.md** updated: Merged inbox decision, marked as superseding the re-bind portion of Don-8 and Edsger v1.2.
- **Code changes:** `OptionClickRenameController` now captures fresh identity at commit time. Re-bind matching removed from `JSONFileSpaceNameStore`.

## Test Results

- Baseline: 21 tests, 0 failures (pre-don-9 v1.2)
- After fixes: 24 tests, 0 failures
- +3 tests added to cover drift and re-bind retirement scenarios

## Impact

- Watermark names now persist correctly across multiple Space visits in same session.
- User no longer sees Space X display a previously renamed label from Space Y.
- UNNAMED fallback applies only on true ambiguity, not on stale re-bind edge cases.
